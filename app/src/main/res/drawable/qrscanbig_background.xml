<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="2dp">

    <!-- Fondo blanco para asegurar que el color se aplique correctamente -->
    <item android:drawable="@android:color/white" />

    <!-- Imagen original de entrada -->
    <item android:drawable="@drawable/qrscanbig" />

    <!-- Máscara de color para aplicar tinte -->
    <item>
        <bitmap android:src="@drawable/qrscanbig"
            android:tint="@color/colorPrimario"
            android:gravity="fill"/>
    </item>

    <!-- Efecto de fondo seleccionable -->
    <item android:drawable="?android:attr/selectableItemBackground" />

</layer-list>
