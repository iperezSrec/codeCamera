<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="2dp">

    <item android:drawable="@drawable/configuration" />

    <!-- Fondo blanco para asegurar que el color se aplique correctamente -->
    <item android:drawable="@android:color/white" />


    <!-- Máscara de color para aplicar tinte -->
    <item>
        <bitmap android:src="@drawable/configuration"
            android:tint="@color/colorSecundario"
            android:gravity="fill"/>
    </item>

    <item android:drawable="?android:attr/selectableItemBackground" />

</layer-list>